spring:
  ai:
    openai:
      api-key: offline-tests-no-provider-calls
    model:
      chat: none
      embedding: none
app:
  visitor-enabled: true
  catalog:
    embedding:
      mode: fake
    seed:
      enabled: false
